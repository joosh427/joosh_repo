var aggTask = new GlideAggregate('task');
aggTask.addAggregate('COUNT');
aggTask.addActiveQuery();
aggTask.query();

while (aggTask.next()) {
    var count = aggTask.getAggregate('COUNT');
    gs.info(count);
}
